* events subsystem, 3 add buttons should be line break separated so they are all left justified regardless of cahta pane window size
* same forf manage sources and hide/show
* should have a horizontal line break between the 3 add buttons section and the manage sources section
* add event prompt should be titled Date of Single Event, with prompt "Format as DD, MM DD, or MM DD YYYY:"
* add monthtly Date of Monthly Event, with prompt Format as DD:
* add yearly Date of Yearly Event, with prompt Format as MM DD:
* when hiding or showing event or source, don't re-output !cal view. Just include the current small line confirming hidden

* weather subsystem
	* please look into the temperature ranges and ensrue they make sense. I am currently looking at a Temperate River Valley Swamp in Early Autumn that is snowing. That makes no sense. Please, full evaluation of the sense of the large system
	* Forecast cells.
		* ToD view is okayt for now
		* forecast view is awful. cells are super tall.
			* need a horizontal line break under the numerical date
			* the emoji is too clsoe to the numerical date
			* the L H temperatures are on different lines. they should be oriented on the same line, left and right centered. that would split the individual day into two columns for that specific L H row, with the l and h centered in their individual column cells
			* no need for all that dead space below the L H![[Screenshot 2026-04-01 at 10.24.34 AM.png]]
			* below the LH, just need a section ofthe cell, centerd, that says Low Medium High or None for the player knowlegde for that specific day. We store that infgor in a flag
			* forecast button works right, but the forecast is also being shown in the basic view. we can remove it from that view and gate it behind the forecast button
			* remove the lock buttons. lock means nothing. remove that function entirely actually. we never need to lock
			* management menu should be 