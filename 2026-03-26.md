> Agent note: struck-through items are completed.

* ~~primary !cal view, instead of additional options I want "Subsystems"~~
* ~~primary !cal view I want at the top, above the minical, "Today's Calendar"~~
* ~~!cal events~~
	* ~~has broken html on the additional ranges button~~
	* ~~has broken html on the add event, monthly, and years buttons~~
	* ~~sources should be "Manage Sources"~~
	* ~~Hide/Show list looks good, but at least one (Day of Cleansing Fire) has the wrong swatch color on it, in the list. the color fill once in the minical is good~~
	* ~~Hide/Show list doesn't show events tied to a hidden source at all. i'd like source management to behave more like a bulk hide or bulk show of the individual events than a true hiding or disabling or enabling of the source. so suppressing a source doesn't actually disable anything, it just grabs each tagged event and suppresses it. and you can "enable" a "disabled" source, which just marks each event to shown. Bulk action for convenience, but not functionally different than individually hitting each event. Still shift all the hidden events to the bottom of the list, as you current do~~
	* ~~This is different from alternate calendar "sources", which I never want to cross-pollute the systems.~~
	* ~~Index number feels antiquated. we have buttons that directly work. Can remove that column.~~
	* ~~Add a column for source. keep sorting order chronological.~~
	* ~~make sure the table resizes correctly. I worry that it has fixed widths which can get screwy on some browsers and hide the action button without forcing a slider bar to be created~~
* ~~Events primary view, additional ranges lists Full Calendar year, rolling 12 months, specific month. All good. But then it lists every "upcoming" month in that menu. Which is not good. Let's add a 4th option to "Upcoming Month", that opens a submenu with those choices in chronological order, named as Month-Name YYYY era label, non-inclusive of current month. I assume if they select the following month (current month + 1), and the current date is within  the one week timeline of that future month, we will render the week strip using that display pathway. Which should always be used whenever rendering an adjacent month.~~
* ~~Additional Ranges drop down menu second bug: Specific Month choice errors and displays this message, which I assume is an error:~~
	* ~~Calendar Jump Syntax~~

`!cal ?\{Month}` does not map cleanly to a month view.

Top-level `!cal`, `!cal show`, and `!cal send` jumps render whole months or years.

Use a month name, a month plus year, or a full date that includes a month:

`!cal Zarantyr`  
`!cal Zarantyr 998`  
`!cal Rhaan 14`  
`!cal next month`  
`!cal this year`

Bare day-only inputs like `!cal 14` or `!cal 1st` are not supported here.

* ~~it seems plausible that the extra months in the primary drop down were actually intended to be in that secondary drop down under "additional months". But let's turn this into a learning thing. Specific Month will open up a query box prompting input, with a default entry of the immediately upcoming month in monthname yyyy format to show syntax~~
* upcoming month is still broken, doesn't create a new submenu when selecting it, and all it's options are in the main menu. might be a html issue that needs an insert character before resolving the character kind of thing to make these nested drop downs.
	* upcoming month should also have #-of-months total choices, so the current month of the following calendar year would be the final choice.
* events main view, Add event should be "add single event". similarly, Add Monthly Event, and Add Yearly Event. Each with a line break between them so they are on separate lines, and not on one line.
	* also add a horizontal line beneath that, above Manage Sources.
	* Manage Sources should be Manage Event Sources
	* Hide/Show should be Hide/Show Event
	* Line break between those as well
	* In the events main view, where we list the current date, please prefix it with "*Current Date:* ", followed by a space, to make it explicit that it's the actively current date even when the minical shows something separate
* Weather Main View
	* Date header bar needs contrasting text. Using the month color is good but the contrast can be poor. Consider using an contrasted outline border around the text.
	* Same for the weekday names in the forecasted view. need contrasting text border
	* forecast cells are screwy, and Low and High are on different vertical levels within the larger cell. likely in their own subcells, while they should be left and right justified in the same level.
	* forcast large cell needs visual line breaks between the date subcell, the emoji subcell, and the L / H subcell.
	* huge dead space at the bottom of the large subcell. lets tighten it up.
	* at the bottom of the cell, below the L/H part, place a line for Low Medium or High, to reflect what player knowledge reveal tag is saved for that day. same for player view as well, so they know what level of knowlege they have for that cell
	* please check how this makes sense for Temperate River Valley Swamp:
		* |0-4a|4-8a|8-12p|12-4p|4-8p|8-12a|
|---|---|---|---|---|---|
|🌧️<br><br>70F|🌧️<br><br>59F|🌧️<br><br>48F|🧊<br><br>42F|🌧️<br><br>51F|🌧️<br><br>50F|

* just seems like a really weird swing that midnight is 70F while it drops to it's coldest at 2pm. Is the tod modifier designed right? is the od modifer using the newest -5 - +15 temp scale?
* just get rid of lock function entirely. both buttons and the function itself.
* weather management menu
	* change button title to Weather System Management
	* Reseed should be "Erase and Reset System" and should be the last option in the menu.
		* Message afterward should be "All weather information erased and system reset."
	* Reroll today doesn't need to be in the menu. we have a button
	* Same for history button
	* Reset weather should be "Regenerate Weather" and should be the first option in the menu.
		* Confirmation message should be "Weather regenerated. Player knowledge and reveal tags preserved."
	* Do those make sense? The implication of the words was backwards. Reset should be full reset. Regenerate is more like reseed, but I like regenerate better as it's more obvious.
	* Menu order should be Regenerate Weather, Toggle Weather, Toggle Extreme Hazards, Toggle Mechanics, Erase and Reset System.
* below the main minical it should be History, line break, GM Forecast, line break, send revealed.