> Agent note: struck-through items are completed.

* ~~primary !cal view, instead of additional options I want "Subsystems"~~
* ~~primary !cal view I want at the top, above the minical, "Today's Calendar"~~
* ~~!cal events~~
	* ~~has broken html on the additional ranges button~~
	* ~~has broken html on the add event, monthly, and years buttons~~
	* ~~sources should be "Manage Sources"~~
	* ~~Hide/Show list looks good, but at least one (Day of Cleansing Fire) has the wrong swatch color on it, in the list. the color fill once in the minical is good~~
	* ~~Hide/Show list doesn't show events tied to a hidden source at all. i'd like source management to behave more like a bulk hide or bulk show of the individual events than a true hiding or disabling or enabling of the source. so suppressing a source doesn't actually disable anything, it just grabs each tagged event and suppresses it. and you can "enable" a "disabled" source, which just marks each event to shown. Bulk action for convenience, but not functionally different than individually hitting each event. Still shift all the hidden events to the bottom of the list, as you current do~~
	* ~~This is different from alternate calendar "sources", which I never want to cross-pollute the systems.~~
	* ~~Index number feels antiquated. we have buttons that directly work. Can remove that column.~~
	* ~~Add a column for source. keep sorting order chronological.~~
	* ~~make sure the table resizes correctly. I worry that it has fixed widths which can get screwy on some browsers and hide the action button without forcing a slider bar to be created~~
* Events primary view, additional ranges lists Full Calendar year, rolling 12 months, specific month. All good. But then it lists every "upcoming" month in that menu. Which is not good. Let's add a 4th option to "Upcoming Month", that opens a submenu with those choices in chronological order, named as Month-Name YYYY era label, non-inclusive of current month. I assume if they select the following month (current month + 1), and the current date is within  the one week timeline of that future month, we will render the week strip using that display pathway. Which should always be used whenever rendering an adjacent month.
* Additional Ranges drop down menu second bug: Specific Month choice errors and displays this message, which I assume is an error:
	* Calendar Jump Syntax

`!cal ?\{Month}` does not map cleanly to a month view.

Top-level `!cal`, `!cal show`, and `!cal send` jumps render whole months or years.

Use a month name, a month plus year, or a full date that includes a month:

`!cal Zarantyr`  
`!cal Zarantyr 998`  
`!cal Rhaan 14`  
`!cal next month`  
`!cal this year`

Bare day-only inputs like `!cal 14` or `!cal 1st` are not supported here.

* it seems plausible that the extra months in the primary drop down were actually intended to be in that secondary drop down under "additional months". But let's turn this into a learning thing. Specific Month will open up a query box prompting input, with a default entry of the immediately upcoming month in monthname yyyy format to show syntax
* 